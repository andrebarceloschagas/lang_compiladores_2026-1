# AST esperada — Exemplo 3
# Arquivo fonte: exemplo3_condicional.minilang
# Gerado por: main_parser.py
# =====================================================
# Código fonte:
#
#   var idade: int
#   var maior: bool
#
#   leia(idade)
#
#   se (idade >= 18) entao
#     maior = verdadeiro
#     escreva("Maior de idade")
#   senao
#     maior = falso
#     escreva("Menor de idade")
#   fim
# =====================================================

Program
├── VarDecl: idade : int
├── VarDecl: maior : bool
├── Leia: idade
└── IfElseStmt
    ├── Cond:
    │   └── BinOp(>=)
    │       ├── Ident(idade)
    │       └── Int(18)
    ├── Then:
    │   └── Block
    │       ├── Assign: maior
    │       │   └── Bool(verdadeiro)
    │       └── Escreva
    │           └── Str("Maior de idade")
    └── Else:
        └── Block
            ├── Assign: maior
            │   └── Bool(falso)
            └── Escreva
                └── Str("Menor de idade")

# =====================================================
# Nós na AST: 20
# Erros sintáticos: 0
# =====================================================
